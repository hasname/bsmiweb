<!doctype html>
<html lang="zh-Hant-TW">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>統編 <%= taxId %> — BSMI 檢驗標識查詢</title>
<meta name="description" content="統編 <%= taxId %> 的 BSMI 檢驗標識列表，共 <%= registrations.length %> 筆">
<style>
body { font-family: sans-serif; margin: 2em; }
table { border-collapse: collapse; width: 100%; margin-top: 1em; }
th, td { border: 1px solid #ccc; padding: 0.5em; text-align: left; }
th { background: #f5f5f5; }
</style>
</head>
<body>
<h1>統編 <%= taxId %></h1>
<p>共 <%= registrations.length %> 筆檢驗標識</p>
<table>
<thead>
<tr>
  <th>標識號碼</th>
  <th>申請人</th>
  <th>電話</th>
</tr>
</thead>
<tbody>
<% for (const reg of registrations) { %>
<tr>
  <td><a href="/bsmi/<%= reg.id %>"><%= reg.id %></a></td>
  <td><%= reg.applicant %></td>
  <td><%= reg.phone %></td>
</tr>
<% } %>
</tbody>
</table>

<h2>被授權資料</h2>
<% if (authorizations.length === 0) { %>
<p>無被授權資料。</p>
<% } else { %>
<table>
<thead>
<tr>
  <th>授權證號</th>
  <th>證書編號</th>
  <th>授權人名稱</th>
  <th>主型式</th>
  <th>被授權人名稱</th>
  <th>授權有效時間</th>
</tr>
</thead>
<tbody>
<% for (const auth of authorizations) { %>
<tr>
  <td><%= auth.id %></td>
  <td><%= auth.certificateId %></td>
  <td><%= auth.authorizerName %></td>
  <td><%= auth.mainModel %></td>
  <td><%= auth.authorizeeName %></td>
  <td><%= auth.validDate %></td>
</tr>
<% } %>
</tbody>
</table>
<% } %>
</body>
</html>
